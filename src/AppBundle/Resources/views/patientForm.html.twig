{% form_theme patientForm 'bootstrap_3_layout.html.twig' %}

{% block reservation_form %}

    {{ form_start(patientForm) }}
    {{ form_widget(patientForm) }}
    {{ form_end(patientForm) }}

{% endblock %}