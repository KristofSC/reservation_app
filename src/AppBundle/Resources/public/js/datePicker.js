$(function () {
            $(".datepicker").datepicker({
                    startDate: '0d',
                    endDate: '2018-03-13',
                    format: 'yyyy-mm-dd'
                }
            );
        $(".datepicker_range").datepicker({
                startDate: '0d',
                format: 'yyyy-mm-dd'
            }
        );
        }
    );
